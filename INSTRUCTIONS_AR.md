# تعليمات تشغيل موقع MONA BEAUTY 💄

## 📋 الخطوات الأساسية

### 1️⃣ تثبيت المكتبات (أول مرة فقط)
افتح Command Prompt في مجلد المشروع واكتب:
```
npm install
```

### 2️⃣ تشغيل المشروع
```
npm run dev
```

⏳ انتظر حتى ترى الرسالتين:
- ✅ API server running at http://localhost:3001
- ✅ Local: http://localhost:5173

### 3️⃣ فتح الموقع
افتح المتصفح واكتب: `http://localhost:5173`

---

## 🔐 الدخول للوحة التحكم (Admin Dashboard)

### الطريقة الأولى - من الرابط مباشرة:
اكتب في المتصفح: `http://localhost:5173/admin/login`

### الطريقة الثانية - من URL الموقع:
اكتب في شريط العنوان: `http://localhost:5173/admin`
وسيحولك تلقائياً لصفحة تسجيل الدخول

### بيانات الدخول:
```
Email: admin@monabeauty.com
Password: admin123
```

بعد تسجيل الدخول ستدخل مباشرة للوحة التحكم

---

## 🛠️ حل المشاكل

### ❌ المشكلة: "Cannot GET /"
**السبب:** السيرفر مش شغال أو الفرونت اند مش شغال  
**الحل:** 
1. اقفل الـ terminal
2. شغل تاني بـ `npm run dev`
3. انتظر الرسالتين ✅

### ❌ المشكلة: "MongoDB unavailable, using in-memory fallback"
**السبب:** مفيش اتصال بقاعدة البيانات  
**الحل:**
1. افتح ملف `.env` وتأكد من رابط MongoDB صحيح
2. اتأكد من الإنترنت شغال
3. في MongoDB Atlas -> Network Access -> اضغط "Add IP Address" -> اختار "Allow Access from Anywhere" (0.0.0.0/0)
4. احفظ التغييرات واستنى دقيقة
5. اعمل restart للسيرفر

### ❌ المشكلة: لما بضغط على منتج مش بيفتح صفحة التفاصيل
**الحل:** تم إصلاح المشكلة! الآن الصفحة بتجيب البيانات من MongoDB

### ❌ المشكلة: زرار "Proceed to checkout" مش بيشتغل
**الحل:** تأكد إن السيرفر شغال على المنفذ 3001 والفرونت على 5173

### ❌ المشكلة: بحذف منتج من الادمن ومش بيتحذف
**السبب:** ممكن المنتج عنده ID مختلف أو قاعدة البيانات مش متصلة  
**الحل:**
1. افتح Console في المتصفح (F12)
2. شوف الأخطاء
3. لو في مشكلة في MongoDB، اتبع خطوات حل مشكلة MongoDB فوق
4. اعمل refresh للصفحة بعد الحذف

---

## 📁 معلومات مهمة عن المشروع

### الصفحات الموجودة:
- **/** - الصفحة الرئيسية
- **/products** - كل المنتجات
- **/products/[id]** - تفاصيل منتج معين
- **/cart** - سلة المشتريات
- **/checkout** - صفحة الدفع
- **/wishlist** - المفضلة
- **/admin/login** - تسجيل دخول الادمن
- **/admin** - لوحة التحكم

### المنافذ (Ports):
- **3001** - API Server (الباك اند)
- **5173** - Frontend (الفرونت اند)

### ملف `.env` يحتوي على:
- رابط MongoDB
- بيانات الادمن (Email & Password)
- Admin Token

---

## ⚙️ أوامر إضافية

```cmd
# تشغيل API Server فقط
npm run dev:api

# تشغيل Frontend فقط
npm run dev:vite

# بناء المشروع للنشر
npm run build

# معاينة المشروع المبني
npm run preview
```

---

## 🔄 خطوات التأكد من كل شيء شغال

1. ✅ افتح terminal
2. ✅ اكتب `npm run dev`
3. ✅ انتظر رسالة API server running
4. ✅ انتظر رسالة Local: http://localhost:5173
5. ✅ افتح المتصفح على http://localhost:5173
6. ✅ اذهب لـ http://localhost:5173/admin/login
7. ✅ سجل دخول بالبيانات الموجودة فوق
8. ✅ جرب تضيف منتج أو تحذف منتج
9. ✅ ارجع للموقع وشوف المنتجات

---

## 📝 ملاحظات

- **لو عايز تشتغل على المشروع:**  
  افتح المشروع في VS Code أو أي محرر نصوص واشتغل براحتك

- **لو حصل خطأ:**  
  شوف الـ Console في المتصفح (اضغط F12) وشوف الرسالة

- **لو عايز تغير بيانات الادمن:**  
  غير في ملف `.env` السطور:
  ```
  ADMIN_EMAIL=...
  ADMIN_PASSWORD=...
  ```

---

## 🎯 الخلاصة السريعة

```
npm install          → مرة واحدة بس
npm run dev          → كل مرة عايز تشغل المشروع
Ctrl+C               → لو عايز توقف السيرفر
```

✨ **بالتوفيق!** ✨
